import CMSAdminPage from "../admin/page";

export const metadata = {
  title: "CMS Control Plane — AURATECH Studio",
  description: "Manage content models and API collections for the studio.",
};

export default function CMSPage() {
  return <CMSAdminPage />;
}
